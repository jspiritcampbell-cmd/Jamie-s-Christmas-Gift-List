
---

## 7) `src/app/layout.js`

```js
export const metadata = {
  title: "Christmas Gift Recommender",
  description: "Gift recommendations based on demographics (free API, deploys on Vercel)."
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
